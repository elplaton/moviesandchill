# URL del servidor Movies&Chill para el paquete de LG (webOS). Misma regla que en .env.tizen:
# apunta directamente al backend (FastAPI en :8000). La TV debe poder llegar a esta IP.
VITE_API_BASE=http://192.168.1.46:8000
